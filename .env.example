# LINE credentials
LINE_CHANNEL_SECRET=YOUR_LINE_CHANNEL_SECRET
LINE_CHANNEL_ACCESS_TOKEN=YOUR_LINE_CHANNEL_ACCESS_TOKEN

# Optional: public CSV url exported from Google Sheets (File -> Share -> Anyone with the link; then File -> Download -> CSV)
DATA_CSV_URL=

# Local CSV fallback (used if DATA_CSV_URL is empty or fails)
LOCAL_CSV_PATH=data/sample_programs.csv

# App
PORT=8000
